version: "3.9"
services:
  sentinel:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    volumes:
      - sentinel-data:/tmp
    restart: unless-stopped

volumes:
  sentinel-data:
